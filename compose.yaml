services:
  irc-bot:
    image: ghcr.io/kernicde/freespawn-irc-bot:latest
    container_name: irc-bot-freespawn
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/bot/data
    networks:
      - freespawn-net

networks:
  freespawn-net:
    external: true
